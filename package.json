{
  "name": "koutei-zu",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "工程図 - 建築CG制作スケジュール可視化アプリ",
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/check-ui-rules.mjs && npm test",
    "test": "node --test scripts/tests/*.test.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "firebase": "^10.13.0",
    "lucide-react": "^0.383.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "gh-pages": "^6.1.1",
    "vite": "^5.4.0"
  }
}
