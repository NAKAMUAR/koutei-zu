rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 匿名認証済みのユーザーのみ読み書き可能
    // ワークスペースIDを知っている人だけがそのデータにアクセスできる
    match /workspaces/{workspaceId}/data/{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
